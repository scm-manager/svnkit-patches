# HG changeset patch
# Parent 3c418c54c3244f8b01d921a98336aad9b91f6827
change group and build jar of svnkit-dav

diff -r 3c418c54c324 build.gradle
--- a/build.gradle	Wed May 29 19:51:15 2013 +0200
+++ b/build.gradle	Mon Jun 03 18:57:28 2013 +0200
@@ -116,13 +116,13 @@
 }
 
 project(':svnkit-dav') {
-    apply plugin: 'war'
+    // apply plugin: 'war'
 
     dependencies {
         compile project(path: ':svnkit')
         compile 'javax.servlet:servlet-api:2.5'
     }
-    artifacts { maven war }
+    artifacts { maven jar, sourcesJar }
 }
 
 project(':svnkit-test') {
@@ -156,10 +156,11 @@
 
         scripts project(path: ':svnkit-cli', configuration: 'scripts')
 
-        osgi project(path: ':svnkit-osgi', configuration: 'archives')
-        osgi 'com.trilead:trilead-ssh2-osgi:1.0.0-build216'
-        osgi 'org.tmatesoft.sqljet:sqljet-osgi:1.1.7'
-        osgi 'net.java.dev.jna:jna-osgi:3.4.0'
+        // skip osgi build, because of dependency problems
+        // osgi project(path: ':svnkit-osgi', configuration: 'archives')
+        // osgi 'com.trilead:trilead-ssh2-osgi:1.0.0-build216'
+        // osgi 'org.tmatesoft.sqljet:sqljet-osgi:1.1.7'
+        // osgi 'net.java.dev.jna:jna-osgi:3.4.0'
     }
 }
 task publish(dependsOn: [':svnkit-distribution:buildAll']) {
diff -r 3c418c54c324 settings.gradle
--- a/settings.gradle	Wed May 29 19:51:15 2013 +0200
+++ b/settings.gradle	Mon Jun 03 18:57:28 2013 +0200
@@ -1,4 +1,4 @@
-rootProject.name = 'org.tmatesoft.svnkit'
+rootProject.name = 'sonia.svnkit'
 
 include 'svnkit'
 include 'svnkit-cli'
diff -r 3c418c54c324 svnkit-dav/build.gradle
--- /dev/null	Thu Jan 01 00:00:00 1970 +0000
+++ b/svnkit-dav/build.gradle	Mon Jun 03 18:57:28 2013 +0200
@@ -0,0 +1,11 @@
+jar {
+    enabled = true 
+}
+
+uploadMaven {
+    repositories.mavenDeployer {
+        pom.whenConfigured { p ->
+            p.dependencies.clear()
+        }
+    }
+}
